# What the server needs. Nothing here touches audio hardware, so the deploy
# build stays small and cannot fail on a missing sound library.
fastapi>=0.110
uvicorn[standard]>=0.27
requests>=2.31
python-dotenv>=1.0.0